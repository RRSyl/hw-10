const config= {
    development: {
        username: "postgres",
        password: "postgres",
        database: "hw-10",
        host: "localhost",
        dialect: "postgres"
    }
}

module.exports = config;